import React from 'react';
import { Typography } from '@mui/material';

const ReviewList: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Review List</Typography>
      <Typography variant="body1">View all your restaurant reviews here.</Typography>
    </div>
  );
};

export default ReviewList;
