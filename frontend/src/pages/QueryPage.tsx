import React from 'react';
import { Typography } from '@mui/material';

const QueryPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Run a Query</Typography>
      <Typography variant="body1">Ask questions about your dining experiences.</Typography>
    </div>
  );
};

export default QueryPage;
